// Deprecated — replaced by AppPicture (chatui-aligned shape).
// This file is left as a re-export shim so any stray imports keep
// working until they are migrated.
export { AppPicture as AppImage } from "./AppPicture";
